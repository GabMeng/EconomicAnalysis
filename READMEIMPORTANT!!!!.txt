We cleaned the data directly inside the excel sheets, so it is important to use the excel files inside this folder "sec.xlsx" "index.xlsx", so just use the path from where you saved the two cleaned databases. 

Every time we are asked to compute the exercise with the daily and the montlhy setting we provide the function, where there are two paramaters to change: 1) the df that could be df_daily df_monthly, and the 2) frequencies that are frequency=252 or frequency=12. 